import React, { FC } from 'react'

import { Background } from '../../components/common/utils/layout'
import { Title } from '../../components/common/utils/typography'

export const Studios: FC = () => {
  return (
    <Background>
      <Title>Studios</Title>
    </Background>
  )
}